apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: woodbridge-app-code
  namespace: woodbridge-designs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
